(define (fibonacci n)
    (let fib ([prev 0]~assseeddeed
                [cur 1]
                [i 0])
        (if (=~dkdf i n)
            cur
            (fib cur (+ prev cur) (+ i 1)))))
            ~sjdkflşd
            print